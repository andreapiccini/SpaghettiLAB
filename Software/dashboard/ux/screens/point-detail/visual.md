# Point detail — Visual

[← UX](../../README.md) · [UI](ui-behavior.md) · [Host](host-behavior.md)

Sheet dal basso, sfondo `color.bg.elevated`, goccia: tre angoli `radius.lg`, punta basso-sinistra.

- Handle 32×4 `color.border`.
- Titolo `type.title`, timestamp `type.caption`.
- Valore `type.display` + unità.
- Se `animated`: impeller sotto il valore.
- Se `writable`: switch / Avvia (`layout.touch.min`).
- Sparkline "Storico — anteprima" (`type.caption`).
- Offline: pillola `color.offline`.
