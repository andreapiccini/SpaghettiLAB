export const NodeRedDeployErrorCode = {
  REVISION_CONFLICT: "node-red-deploy.revision_conflict",
  AUTH_FAILED: "node-red-deploy.auth_failed",
  REMOTE_ERROR: "node-red-deploy.remote_error",
} as const;
