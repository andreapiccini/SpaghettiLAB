# @spaghettilab/react-flow-adapter

Placeholder package created by S011 to establish the workspace boundary. Real content
(bidirectional Domain ↔ React Flow adapter, `EditorModel` wiring) is implemented by
task S043 — see `../../../roadmap/react-flow-v1/tasks/S043-react-flow-adapter.md`.

Unlike `@spaghettilab/domain`, this package is allowed to depend on React and
`@xyflow/react` — translating between them is its entire purpose.
