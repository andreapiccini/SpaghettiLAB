# Optional USB passthrough for flashing from a Linux host.
# Usage: docker compose -f compose.yaml -f compose.linux.yaml run --rm dev
services:
  dev:
    devices:
      - ${ESP32_PORT:-/dev/ttyACM0}:${ESP32_PORT:-/dev/ttyACM0}
