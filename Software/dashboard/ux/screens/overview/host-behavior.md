# Overview — Host behavior

[Visual](visual.md) · [UI](ui-behavior.md)

`GET /v1/systems/{id}`, `GET points`, stream `system_status` e `point_updated`.
Allarme = punto `visualHint=status` con valore true o `visualState=alarm`.
