name: dashboard_host
description: Protocol V1 adapter and EdgeHost. Under HostPort — not used by Flutter screens.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  dashboard_domain:
    path: ../dashboard_domain
  mqtt_client: ^10.8.0
  typed_data: ^1.3.2
  http: ^1.2.2

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
