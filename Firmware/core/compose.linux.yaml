# Optional USB passthrough for running west flash manually inside Docker on Linux.
# Usage: docker compose -f compose.yaml -f compose.linux.yaml run --rm dev
services:
  dev:
    devices:
      - ${PORT:-/dev/ttyACM0}:${PORT:-/dev/ttyACM0}
